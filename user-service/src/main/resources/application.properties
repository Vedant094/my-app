spring.application.name=user-service
server.port=8081

# MySQL Config
spring.datasource.url=jdbc:mysql://localhost:3306/food_user_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Coforge@123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=super-secret-key-super-secret-key-super-secret-key
jwt.expiration=86400000

# Disable Spring Cloud Config checks (because you're not using config server)
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

